{
  "name": "mahjong-trainer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gh-pages": "gh-pages -d dist -b gh-pages",
    "deploy": "tsc && npm run build && npm run gh-pages"
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "sygnal": "^4.2.0",
    "terser": "^5.46.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^6.4.1"
  }
}
